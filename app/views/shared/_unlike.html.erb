<%= form_for(current_user.likes.where(showcase_id: showcase.id).first,
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "unlike", class: "btn btn-large btn-primary" %>
<% end %>
